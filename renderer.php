<?php
namespace mod_stream\output;

defined('MOODLE_INTERNAL') || die();

class renderer extends \plugin_renderer_base {
    // Hier keine eigenen Methoden nötig, außer du möchtest Custom-Rendering überschreiben.
}

